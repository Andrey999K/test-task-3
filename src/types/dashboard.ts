export type StatusDataType = {
  name: string;
  value: number;
  color: string;
};

export type GenderDataType = {
  name: string;
  value: number;
};

export type DashboardStatsType = {
  total: number;
  active: number;
  pending: number;
  archived: number;
};